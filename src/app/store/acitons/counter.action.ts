import { createAction } from '@ngrx/store';

export const countIncrement = createAction('[Count Component] Count Increment');
export const countDecrement = createAction('[Count Component] Count Decrement');
